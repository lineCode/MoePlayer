const config = {
	// 服务器
	host: 'http://localhost',
	// 端口
	port: 5000,
	// 是否开启调试面板
	debug: true,
	// 是否可以调整窗口大小
	resizable: false,
	// 窗口宽度
	width: 1000,
	// 窗口高度
	height: 700,
	// 每页显示的歌曲数
	num_per_page: 15
};

module.exports = config;
